---
interface Props {
  href: string;
  label: string;
  variant?: "primary" | "secondary";
}
const { href, label, variant = "secondary" } = Astro.props;
---

<a class:list={["button", variant]} href={href} aria-label={label}><slot /></a>
