import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://taskinoz.github.io",
  base: "/Impression-Eyes-Reborne",
  output: "static",
  trailingSlash: "always",
});
